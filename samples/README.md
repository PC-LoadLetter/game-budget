# Sample journals

| File | In git? | Purpose |
|------|---------|---------|
| `fixture.dat` | Yes | Small synthetic ledger for tests and smoke checks (Alpha/Beta, no real data) |
| `journal.dat` | **No** (gitignored) | Optional local copy of a real household journal — never commit |

Tests and CI use `fixture.dat` only.
